# Site
title: XiDOS
remote_theme: just-the-docs/just-the-docs
description: A POSIX compatibility layer for MS-DOS / FreeDOS — Cygwin, but for DOS.
color_scheme: dark

# Navigation
search_enabled: true
aux_links:
  "XiDOS on GitHub":
    - "https://github.com/Arawn-Davies/xidos"

# Plugins (whitelisted by GitHub Pages)
plugins:
  - jekyll-relative-links

# Don't process repo plumbing as site content.
exclude:
  - vendor
